diff --git a/pcsx2/Vif_HashBucket.h b/pcsx2/Vif_HashBucket.h
index 56b649e..1234567 100644
--- a/pcsx2/Vif_HashBucket.h
+++ b/pcsx2/Vif_HashBucket.h
@@ -75,6 +75,10 @@ public:
 	__fi nVifBlock* find(const nVifBlock& dataPtr)
 	{
 		nVifBlock* chainpos = m_bucket[dataPtr.hash_key];
+
+		if (chainpos == nullptr)
+			return nullptr;
+
 		for (;;)
 		{
 			if (chainpos->key0 == dataPtr.key0 && chainpos->key1 == dataPtr.key1)
@@ -98,6 +102,14 @@ public:
 		// Warning there is an extra +1 due to the empty cell
 		// Performance note: 64B align to reduce cache miss penalty in `find`
 		m_bucket[b] = (nVifBlock*)pcsx2_aligned_realloc(m_bucket[b], sizeof(nVifBlock) * (size + 2), 64, sizeof(nVifBlock) * (size + 1));
+
+		if (m_bucket[b] == nullptr)
+		{
+			Console.Error("Vif_HashBucket: Failed to allocate memory for bucket");
+			return;
+		}
+
 		// Replace the empty cell by the new block and create a new empty cell
 		memcpy(&m_bucket[b][size++], &dataPtr, sizeof(nVifBlock));
 		memset(&m_bucket[b][size], 0, sizeof(nVifBlock));
@@ -106,6 +114,10 @@ public:
 	u32 bucket_size(const nVifBlock& dataPtr)
 	{
 		nVifBlock* chainpos = m_bucket[dataPtr.hash_key];
+
+		if (chainpos == nullptr)
+			return 0;
 
 		u32 size = 0;
 
@@ -135,6 +147,16 @@ public:
 		for (i = 0; i < HSIZE; i++)
 		{
 			m_bucket[i] = (nVifBlock*)_aligned_malloc(sizeof(nVifBlock), 16);
+
+			if (m_bucket[i] == nullptr)
+			{
+				Console.Error("Vif_HashBucket::reset: Failed to allocate memory");
+				for (int j = 0; j < i; j++)
+					safe_aligned_free(m_bucket[j]);
+				return;
+			}
+
 			memset(m_bucket[i], 0, sizeof(nVifBlock));
 		}
 	}
diff --git a/pcsx2/IopBios.cpp b/pcsx2/IopBios.cpp
index 28548d8..2345678 100644
--- a/pcsx2/IopBios.cpp
+++ b/pcsx2/IopBios.cpp
@@ -416,7 +416,18 @@ namespace R3000A
 		if (iomanX)
 		{
 			fxio_dirent_t* hostcontent = (fxio_dirent_t*)buf;
-			strcpy(hostcontent->name, dir->FileName.c_str());
+			const size_t max_name_len = sizeof(hostcontent->name) - 1;
+			const std::string& src_name = dir->FileName;
+			if (src_name.length() > max_name_len)
+			{
+				Console.Warning("IopBios: Filename truncated (too long: %zu bytes)", src_name.length());
+				strncpy(hostcontent->name, src_name.c_str(), max_name_len);
+				hostcontent->name[max_name_len] = '\0';
+			}
+			else
+			{
+				strcpy(hostcontent->name, src_name.c_str());
+			}
 			host_stat(host_path(Path::Combine(basedir, dir->FileName), true), &hostcontent->stat);
 		}
 		else
@@ -424,7 +435,18 @@ namespace R3000A
 		{
 			fio_dirent_t* hostcontent = (fio_dirent_t*)buf;
-			strcpy(hostcontent->name, dir->FileName.c_str());
+			const size_t max_name_len = sizeof(hostcontent->name) - 1;
+			const std::string& src_name = dir->FileName;
+			if (src_name.length() > max_name_len)
+			{
+				Console.Warning("IopBios: Filename truncated (too long: %zu bytes)", src_name.length());
+				strncpy(hostcontent->name, src_name.c_str(), max_name_len);
+				hostcontent->name[max_name_len] = '\0';
+			}
+			else
+			{
+				strcpy(hostcontent->name, src_name.c_str());
+			}
 			host_stat(host_path(Path::Combine(basedir, dir->FileName), true), &hostcontent->stat);
 		}
diff --git a/pcsx2/DEV9/ATA/ATA_Transfer.cpp b/pcsx2/DEV9/ATA/ATA_Transfer.cpp
index c6cefcf..3456789 100644
--- a/pcsx2/DEV9/ATA/ATA_Transfer.cpp
+++ b/pcsx2/DEV9/ATA/ATA_Transfer.cpp
@@ -334,11 +334,23 @@ void ATA::HDD_ReadAsync(void (ATA::*drqCMD)())
 	nsectorLeft = nsector;
 	if (readBufferLen < nsector * 512)
 	{
 		delete readBuffer;
-		readBuffer = new u8[nsector * 512];
-		readBufferLen = nsector * 512;
+		size_t new_size = nsector * 512;
+		try
+		{
+			readBuffer = new u8[new_size];
+			readBufferLen = new_size;
+		}
+		catch (const std::bad_alloc&)
+		{
+			Console.Error("DEV9: ATA: Failed to allocate read buffer (%zu bytes)", new_size);
+			readBuffer = nullptr;
+			readBufferLen = 0;
+			return;
+		}
 	}
 	waitingCmd = drqCMD;
@@ -378,11 +390,26 @@ void ATA::HDD_ReadSync(void (ATA::*drqCMD)())
 	nsectorLeft = nsector;
 	if (readBufferLen < nsector * 512)
 	{
 		delete[] readBuffer;
-		readBuffer = new u8[nsector * 512];
-		readBufferLen = nsector * 512;
+		size_t new_size = nsector * 512;
+		try
+		{
+			readBuffer = new u8[new_size];
+			readBufferLen = new_size;
+		}
+		catch (const std::bad_alloc&)
+		{
+			Console.Error("DEV9: ATA: Failed to allocate read buffer (%zu bytes)", new_size);
+			readBuffer = nullptr;
+			readBufferLen = 0;
+			if (ioWritePaused)
+			{
+				ioWaitHandle.lock();
+				ioWrite = true;
+				ioWaitHandle.unlock();
+				ioReady.notify_all();
+			}
+			return;
+		}
 	}
 
 	IO_Read();
diff --git a/common/AlignedMalloc.h b/common/AlignedMalloc.h
index ed7d43d..5678901 100644
--- a/common/AlignedMalloc.h
+++ b/common/AlignedMalloc.h
@@ -52,6 +52,12 @@ extern void _aligned_free(void* pmem);
 // AlignedMalloc.cpp's #if !defined(_WIN32) guard left the symbol
 // undefined - the build only ever linked because the mingw pre-d2d1ebc
 // __forceinline behaviour kept the call inlined into a single site).
+//
+// SECURITY NOTE: If _aligned_malloc fails and returns nullptr, the original
+// handle is NOT freed. Callers MUST check the return value and handle failure
+// appropriately. New code should use std::unique_ptr with custom deleters for
+// safer memory management.
+//
 static inline void* pcsx2_aligned_realloc(void* handle, size_t new_size, size_t align, size_t old_size)
 {
 	void* newbuf = _aligned_malloc(new_size, align);
diff --git a/pcsx2/GS/Renderers/Common/GSFastList.h b/pcsx2/GS/Renderers/Common/GSFastList.h
index 46d9e76..7890123 100644
--- a/pcsx2/GS/Renderers/Common/GSFastList.h
+++ b/pcsx2/GS/Renderers/Common/GSFastList.h
@@ -77,6 +77,19 @@ public:
 		// Initialize m_buffer and m_free_indexes_stack as a contiguous block of memory starting at m_buffer
 		// This should increase cache locality and reduce memory fragmentation
 		_aligned_free(m_buffer);
 		m_buffer = (Element<T>*)_aligned_malloc(m_capacity * sizeof(Element<T>) + (m_capacity - 1) * sizeof(u16), 64);
+
+		if (m_buffer == nullptr)
+		{
+			Console.Error("GSFastList: Failed to allocate buffer in clear()");
+			m_capacity = 0;
+			m_free_indexes_stack = nullptr;
+			m_free_indexes_stack_top = 0;
+			return;
+		}
+
 		m_free_indexes_stack = (u16*)&m_buffer[m_capacity];
 
 		// Initialize m_buffer[0], data field is unused but initialized using default T constructor
